import { ProductGroupingPipe } from './product-grouping.pipe';

describe('ProductGroupingPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductGroupingPipe();
    expect(pipe).toBeTruthy();
  });
});
